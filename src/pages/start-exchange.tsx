import Layout from '../components/layout/layout';
import Messages from '../components/start-exchange/start-exchange';

const StartExchangePage = () => {
  return (
    <Layout>
      <Messages />
    </Layout>
  );
};

export default StartExchangePage;
